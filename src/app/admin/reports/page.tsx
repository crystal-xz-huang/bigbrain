import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Reports',
  description: 'View and manage reports',
}

export default function ReportsPage() {
  return <div>Your reports go here!</div>
}
